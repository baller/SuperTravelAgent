# SuperTravel Design System

> 设计方向：旅行手帐简笔画（Travel Sketchbook）  
> 产品形态：以一段 Trip 为中心、由一个 AI 旅行管家持续维护的 Web 产品  
> 状态：MVP 正式界面的设计与实现基线  
> 关联文档：[SuperTravel PRD V2](docs/SUPERTRAVEL_PRD_V2.md)

## 1. Design Intent

SuperTravel 是一位围绕一段旅程持续工作的贴身 AI 旅行管家。产品需要同时传达两种气质：旅行服务的个人温度，以及 Trip State 的可信与秩序。旅行手帐是视觉语言，不是产品定位。

`Trip State` 是唯一事实源，`Trip Workspace` 是产品控制台，Agent 是用户需求与旅行变化的统一语义入口。聊天不存储独立事实，也不得取代时间线和地图。

### Experience Principles

1. **手帐有温度，信息有秩序。** 手绘用于情绪、路线和记忆；时间、预算、状态和操作保持规整。
2. **变化必须看得见。** Agent 修改通过批注、连线和差异预览呈现，应用前后均可理解。
3. **现实优先于装饰。** 冲突、数据新鲜度、假设和失败恢复的层级高于插画。
4. **局部而克制。** 每屏最多一个主插画区域；贴纸、胶带和邮戳只出现在有语义的位置。
5. **始终可继续。** 空、加载、部分成功、取消、失败和离线状态都提供下一步。

### Visual Dials

- 手绘表现：7/10
- 信息密度：6/10，Workspace 为 7/10，今日模式为 4/10
- 布局变化度：5/10
- 动效强度：3/10
- 圆润程度：4/10

## 2. Brand Language

### Personality

- 温暖、好奇、可靠、有条理。
- 像熟悉当地的旅行搭子，不像客服机器人或开发者控制台。
- 文案直接、具体，允许轻微口语感，不卖弄 AI 能力。

### Signature Motifs

- 连续手绘路线、编号地点圆点、地图箭头。
- 行李牌、车票、邮戳、胶带、便签、回形针。
- 火车、相机、雨伞、咖啡、建筑、树木等单线简笔画。
- 铅笔下划线和页边批注，用于解释 Agent 的理解与修改。

### Logo Direction

文字标识使用 `SuperTravel`，图形可由“打开的手帐 + 一条穿页路线”构成。标志必须在 24px 高度仍可识别，提供单色墨线版，不使用机器人头像、星芒或渐变魔法棒。

## 3. Color System

MVP 采用浅色纸张主题。深色主题不进入当前范围，避免只做简单反色而破坏手帐语义。

| Token | Value | Usage |
|---|---:|---|
| `canvas` | `#F6F0E2` | 应用背景、手帐桌面 |
| `paper` | `#FFFCF4` | 主内容、日程页面、弹层 |
| `paper-muted` | `#EFE6D2` | 次级区域、禁用底色 |
| `ink` | `#2F332F` | 主文字、主线稿 |
| `ink-muted` | `#5E655E` | 次级文字、说明；在 paper 上对比度 5.85:1 |
| `line` | `#D8CEBA` | 分隔线、纸张边界 |
| `primary` | `#3F6B57` | 主按钮、选中状态、已确认路线 |
| `primary-hover` | `#315543` | 主按钮悬停、强调文字 |
| `sky` | `#4D7F98` | 信息、天气、候选路线 |
| `coral` | `#C95F4C` | 旅行强调、插画点色，不承载小号白字 |
| `stamp` | `#DDB65A` | 邮戳、收藏、待确认装饰 |
| `success-bg` | `#DDE9DF` | 成功、已完成背景 |
| `warning-bg` | `#F7E2A5` | 风险便签背景 |
| `warning-ink` | `#6F4508` | 风险文字；在 warning-bg 上对比度 6.47:1 |
| `danger` | `#A9423A` | 阻断、删除、失败 |
| `scrim` | `rgba(47,51,47,.48)` | 模态遮罩 |

```css
:root {
  --color-canvas: #f6f0e2;
  --color-paper: #fffcf4;
  --color-paper-muted: #efe6d2;
  --color-ink: #2f332f;
  --color-ink-muted: #5e655e;
  --color-line: #d8ceba;
  --color-primary: #3f6b57;
  --color-primary-hover: #315543;
  --color-sky: #4d7f98;
  --color-coral: #c95f4c;
  --color-stamp: #ddb65a;
  --color-success-bg: #dde9df;
  --color-warning-bg: #f7e2a5;
  --color-warning-ink: #6f4508;
  --color-danger: #a9423a;
  --color-focus: #315543;
}
```

功能状态不得仅依赖颜色：必须同时使用图标、标题和文字。现有 `#1890ff`、蓝紫渐变及散落的十六进制色值均视为旧版样式，不得进入新页面。

## 4. Typography

### Font Families

- 展示与旅行标题：`"LXGW WenKai Screen", "Noto Serif SC", serif`。
- UI 与正文：`"Noto Sans SC", "PingFang SC", "Microsoft YaHei", sans-serif`。
- 时间、价格和里程使用 `font-variant-numeric: tabular-nums`，不额外使用程序员字体。

手写字体只用于 Trip 名称、目的地、日期章和极短批注。表单、按钮、状态、长正文和所有数字必须使用 UI 字体。

| Role | Size / Line height | Weight | Notes |
|---|---|---:|---|
| Display | 44 / 56 | 600 | 首页目的地，最多两行 |
| Page title | 32 / 42 | 600 | Trip 与核心页面标题 |
| Section title | 24 / 34 | 600 | 主分区 |
| Card title | 18 / 26 | 600 | 地点、日期、面板标题 |
| Body | 16 / 26 | 400 | 默认正文 |
| UI label | 14 / 20 | 500 | 按钮、字段、标签 |
| Caption | 12 / 18 | 500 | 来源、更新时间；不得承载关键操作 |

桌面端正文不得小于 14px；窄屏输入框与正文不得小于 16px。长文本宽度限制在 65–75 个英文字符或约 32–38 个汉字。

## 5. Layout, Spacing & Surfaces

### Spacing

使用 4px 基础网格，优先取值 `4 / 8 / 12 / 16 / 24 / 32 / 48 / 64`。组件内边距以 12 或 16px 为主，页面分区间距至少 24px。

### Breakpoints

| Width | Behavior |
|---|---|
| `>= 1280px` | Workspace 三栏：240px / minmax(560px, 1fr) / 360px |
| `1024–1279px` | 左栏 216px + 主时间轴；右栏变为可固定抽屉 |
| `768–1023px` | 日期栏缩为 88px；地图、Agent、冲突使用侧边抽屉 |
| `< 768px` | 单栏响应式 Web；日程优先，地图和 Agent 使用顶层标签或底部面板 |

MVP 不做跨端同步，但必须保证 375px–1440px Web 可用。窄屏不得出现横向页面滚动。

### Surfaces

- `Paper`: 主要工作面，无旋转，1px line 边界。
- `Clip`: 素材卡，可有单个折角或回形针；旋转角度固定为 0°。
- `Sticky`: 澄清、警告和暂存内容；只使用黄色或低饱和蓝色。
- `Overlay`: 地图详情、模态框、Agent 变更预览；允许轻阴影。

圆角采用 `6 / 10 / 16px`。按钮和输入框为 10px，纸张为 16px，标签为全圆角。普通内容不用重阴影，浮层阴影统一为 `0 12px 32px rgba(47,51,47,.14)`。

纸张纹理只允许使用不透明度不高于 2.5% 的无缝噪点，不能覆盖地图、正文和交互控件。胶带不得承担按钮或状态标签的功能。

## 6. Illustration & Iconography

### Doodle Style

- SVG 优先，线帽与拐角均为 round。
- 24px 图标描边 1.75px，大型插画描边 2–2.5px。
- 主线使用 ink；单幅插画最多使用两种点色，填色透明度 12–24%。
- 允许轻微曲线和不完全对称，不允许模拟抖动导致边缘模糊。
- 插画必须支持 `currentColor` 或语义色 Token；不把 Emoji 当作结构图标。

### Usage Levels

1. **品牌插画**：首页和旅行回顾，每屏最多一幅，占首屏面积不超过 25%。
2. **情境插画**：空状态、天气、交通和城市特征，尺寸 80–160px。
3. **功能图标**：Lucide 或自有手绘 SVG，统一 20/24px，保持同一描边语言。

### Map Language

- 底图使用低饱和中性样式，隐藏无关商业 POI。
- 主路线为 4px primary 实线，外加 2px paper 描边以保证底图可读性。
- 候选路线为 3px sky 虚线；受阻路线为 danger 断线并附文字。
- 地点标记使用 28px 编号圆点，编号与时间轴严格对应。
- 步行、公交、打车通过一致的线型和图标区分，不能只靠颜色。

### Initial Asset Set

首批至少准备：打开的手帐、地图折页、行李箱、火车、公交、步行脚印、相机、雨伞、咖啡、餐具、酒店、门票、时钟、预算、锁、警告、完成章、取消章、离线、重试、导出，共 24 个 SVG。

## 7. Motion

| Token | Duration | Usage |
|---|---:|---|
| `fast` | 150ms | hover、press、focus |
| `base` | 220ms | 展开、切换、标签更新 |
| `slow` | 320ms | 抽屉、Patch 预览、地图定位 |

- 使用 `transform` 与 `opacity`，不得动画 width、height、top 或 left。
- 路线可使用一次性描边进入动画；Agent 应用变更时只动画受影响节点。
- 完成章允许 0.98→1 的轻微缩放，禁止弹跳、彩屑和持续漂浮。
- 动画必须可中断，不阻塞操作，并遵循 `prefers-reduced-motion`。

## 8. Core Components

### Navigation

- 全局导航仅包含“旅行”“灵感”“回顾”及当前 Trip 返回入口。
- Desktop 使用文字 + 20px 图标；窄屏最多 4 个顶层入口。
- 当前页面同时通过字重、底线和 `aria-current` 表示，不仅改变颜色。

### Buttons

- 高度至少 44px；小图标按钮的点击区域仍为 44×44px。
- Primary：primary 背景 + paper 文字；每屏只有一个主要动作。
- Secondary：paper 背景 + primary 边框。
- Tertiary：透明背景 + ink；用于局部操作。
- Destructive：danger 背景或 danger 文字，与普通动作分隔。
- Loading 时保留按钮宽度、禁用重复提交，并提供文字状态。

### Inputs & Command Bar

- 始终显示可见标签；placeholder 只提供示例，不充当标签。
- 默认高度 48px，聚焦使用 3px focus 外环。
- Agent Command Bar 固定在 Workspace 主区底部，提示“告诉旅行搭子你想改哪里…”。
- 输入框上方显示当前作用域，如“正在修改：第 2 天下午”；用户可清除作用域。
- 运行时提供明确的“停止”按钮，不以旋转图标替代。

### Trip Cover

- 显示目的地、日期、同行人、计划状态和一幅地点简笔画。
- 可有一个邮戳和一段胶带；不得叠加多个装饰贴纸。
- 封面状态使用文本：“草稿”“待确认”“可出发”“旅行中”“已完成”。

### TripSpec Note

- 以规整便签呈现目的地、日期、预算、同行人、节奏、必去和禁忌。
- 假设使用铅笔下划线与“假设”标签；硬约束使用锁图标。
- 字段修改立即保存，但改变目的地或日期必须确认影响。

### Source Clip

- 显示素材缩略图、来源、证据片段、识别状态和 POI 数量。
- “加入候选”“暂存”“忽略”为显式动作；同名歧义必须就地确认。
- 解析失败保留原素材，展示“重新解析”和“手工添加”。

### Itinerary Item

- 左侧固定时间与路线节点，主体显示地点、时长、费用和原因，右侧放操作。
- 锁定、预约和已完成状态始终可见。
- hover 才出现的动作必须同时可通过焦点或更多菜单访问。
- 拖拽之外提供“移到上一站/下一站/其他日期”的键盘可用操作。

### Route Leg

- 位于两个地点之间，显示交通方式、耗时、距离和数据状态。
- 视觉弱于地点卡，但路线异常时提升为警告便签。
- 实时、缓存、模拟数据使用文字标签并展示更新时间。

### Conflict Note

- `阻断`：danger 左边界 + 错误图标 + 必须处理的原因。
- `警告`：warning-bg + 风险图标 + 影响说明。
- `建议`：低饱和 sky 背景 + 灯泡线稿 + 可选优化。
- 每条冲突提供“查看影响”“预览修复”“保留原样”；阻断不能仅被关闭。

### PlanPatch Preview

- 使用“新增 / 移动 / 替换 / 删除 / 保留”五类明确标签。
- 原方案与新方案可按时间轴对照，不展示原始 JSON。
- 删除使用删除线和 danger 图标；移动使用连接箭头；新增使用 primary 下划线。
- 底部固定“应用变更”“仅应用所选”“保留原计划”。应用后提供撤销。

### Agent Margin Panel

- Agent 是“旅行搭子批注”，不是独立聊天主页。
- 面板包含：理解摘要、进行状态、待确认问题、变更预览和简短解释。
- 不展示模型名、多 Agent 分工、MCP、提示词、Token 或内部思维过程。

### Feedback

- Toast 保留 3–5 秒，使用 `aria-live="polite"`，不得抢夺焦点。
- 需要用户决定的内容不能使用 Toast，必须使用面板或对话框。
- Skeleton 形状应匹配最终卡片；超过 10 秒显示阶段和停止入口。

## 9. Page Templates

### Home

- 目标：让用户在 10 秒内理解“新建 Trip、导入灵感、继续最近旅行”。
- 左侧使用一句价值主张和主 CTA，右侧为打开的手帐主插画。
- 最近 Trip 以 2–4 张封面展示；示例 Trip 明确标记“演示数据”。
- 不展示模型配置、工具列表或空白聊天框。

### Create Trip

- 单页渐进式向导：自然语言输入 → TripSpec 草稿 → 最多 3 个关键澄清 → 确认。
- 页面像一张正在填写的旅行计划页，不使用多层模态框。
- 用户跳过问题时，在确认页集中展示假设。

### Inspiration Inbox

- 左侧是素材入口，中间是 Source Clip 列表，右侧是 POI 候选与地图定位。
- 批量提取显示整体进度和每项状态；单项失败不遮挡其他结果。
- 视觉可借用回形针与折角，仍保持严格网格对齐。

### Trip Workspace

```text
┌──────────────── Trip header / status / budget ────────────────┐
│ Day rail & constraints │ Itinerary notebook │ Map / Agent     │
│                        │ timeline            │ Conflict / Ver. │
│                        │                     │                 │
│                        │ Agent command bar   │                 │
└───────────────────────────────────────────────────────────────┘
```

- Header 保持 64px，展示 Trip 状态和最近保存时间。
- 中栏是唯一主滚动区；左右栏保持独立但避免三重嵌套滚动。
- 地图、Agent、冲突和版本共享右栏标签，同一时间只显示一个。
- 选中行程节点时，地图标记、时间轴和右栏详情同步高亮。

### Today Mode

- 信息优先级：当前事项 → 下一站与出发时间 → 导航/凭证 → 天气 → 余程。
- 主操作为“完成”；“跳过”“延迟”“重排余程”为次级操作。
- 除一个天气简笔画外不放装饰，保证行中快速识别。
- 离线时显示最后更新时间，继续提供已缓存地址、电话和票证。

### Trip Review

- 以完成手帐页表现实际路线、计划偏差、费用和个人反馈。
- 照片不超过 6 张，支持为空；没有照片时使用地点线稿。
- 偏好记忆必须在保存前展示内容与作用范围。

## 10. Agent Experience States

| Run State | User-facing expression | Required action |
|---|---|---|
| `IDLE` | 页边示例指令 | 可直接输入 |
| `UNDERSTANDING` | 铅笔下划线逐项标出日期、对象和约束 | 允许停止 |
| `FETCHING` | 手绘路线进度 + 已找到来源数 | 允许停止；不暴露工具名 |
| `VALIDATING` | 清单逐项检查时间、路程、预算和开放状态 | 允许停止 |
| `WAITING_USER` | 黄色便签，最多 3 个问题 | 回答、跳过或取消 |
| `PREVIEW` | PlanPatch 差异页 | 应用、部分应用或保留原计划 |
| `PARTIAL` | 蓝色便签，列出完成与缺失部分 | 重试缺失项或继续使用当前结果 |
| `SUCCEEDED` | 绿色完成章 + 版本号 | 查看变更或撤销 |
| `CANCELLED` | 中性“已停止”章，说明已保留内容 | 继续、重新开始或关闭 |
| `FAILED` | 红色便签，说明影响与恢复路径 | 重试、改为手工或返回检查点 |

新输入到达时，界面必须明确显示系统判断：补充当前任务、替换当前任务或独立查询。替换操作在取消旧任务后再开始，不能同时显示两个写入进度。

## 11. Global State Matrix

所有核心页面和组件必须覆盖：

- `Empty`：说明价值并提供一个主动作。
- `Loading`：超过 300ms 使用骨架；超过 10 秒显示阶段、耗时和停止。
- `Success`：反馈结果及下一步，不自动跳走。
- `Partial`：保留成功内容，明确缺失项和重试范围。
- `Waiting`：说明系统在等什么，允许跳过或取消。
- `Cancelled`：不回滚已提交状态，标出未完成部分。
- `Failed`：给出原因、影响、已保留内容和恢复动作。
- `Offline/Stale`：继续展示最后可信数据，标注来源和更新时间。
- `Permission denied`：解释需要的权限并提供替代输入方式。
- `Conflict`：阻止危险提交，允许返回修改或比较方案。

## 12. Content & Microcopy

- 使用用户语言：“旅行”“行程”“旅行搭子”“帮我调整”，不使用“Agent 编排”“工具链”“工作流节点”。
- 按钮使用动词与对象：“生成行程”“应用第二天变更”“重试天气查询”。
- 错误文案结构：发生了什么 → 影响什么 → 已保留什么 → 可以怎么做。
- 不说“出错了，请稍后再试”而不提供恢复路径。
- 来源标签固定为“实时”“缓存”“演示数据”；不得用模糊的“AI 推荐”代替事实来源。
- Agent 解释控制在 2–4 句，较长依据放入“查看原因”。

## 13. Accessibility & Performance

- 正文与背景对比度至少 4.5:1，大字号至少 3:1；关键组合已在本规范中验证。
- 所有交互目标至少 44×44px，相邻目标间距至少 8px。
- 焦点顺序与视觉顺序一致；焦点环为 3px focus，并保留浏览器语义。
- 图标按钮必须有可访问名称；动态状态使用 `aria-live`，不依赖颜色。
- 拖拽必须提供键盘替代；地图信息需同步提供列表或文本摘要。
- 图片声明尺寸并懒加载；首屏插画使用 SVG 或 AVIF/WebP，避免布局偏移。
- 页面输入反馈不迟于 100ms；单帧工作控制在约 16ms。
- 支持 200% 文本缩放和 `prefers-reduced-motion`。

## 14. Implementation Guidance

- 新设计 Token 集中在 `apps/web/src/styles.css`，组件只引用语义变量。
- 简笔画资源与组件放在 `apps/web/src/components/`，文件名使用 `PascalCase.tsx`。
- 业务组件建议按 `trip/`、`inspiration/`、`agent/`、`shared/` 分组，不继续扩展单个超大聊天组件。
- 可以复用 Ant Design 的可访问交互行为，但必须通过统一 Theme Token 适配；禁止继续堆叠全局 `!important` 覆盖。
- 字体优先本地 WOFF2 子集，并设置 `font-display: swap`；加载失败时保持稳定回退。
- 插画、纸张纹理和地图样式必须来自可复用资源，不在组件中复制随机 SVG 路径或硬编码颜色。

## 15. Anti-patterns

- 不使用蓝紫渐变、发光边框、玻璃拟态和科技粒子表达 AI。
- 不把所有内容放入悬浮圆角卡片，也不嵌套三层卡片。
- 不随机旋转卡片，不在每张卡上放胶带、邮戳和贴纸。
- 不用手写字体显示长正文、金额、时间和错误信息。
- 不用 Emoji、不同风格图标或粗细不一致的线稿。
- 不把工具调用日志、模型思考和 Agent 数量直接暴露给用户。
- 不在未预览时整页重写行程，不隐藏被删除或移动的内容。
- 不用装饰动效延迟反馈，不让动画阻止取消或输入。

## 16. Definition of Done

一个页面只有满足以下条件才算完成：

- 使用本文件语义 Token，无散落品牌色和任意间距。
- 正常、空、加载、部分成功、失败、取消和离线状态均有设计。
- 375px、768px、1024px、1440px 无横向溢出或被固定元素遮挡。
- 键盘可完成所有主要操作，焦点可见，拖拽有替代入口。
- 正文对比度、触控尺寸、图片替代文字和动态状态播报通过检查。
- 插画用量与线条语言一致，没有 Emoji 或通用 AI 视觉元素。
- Agent 写操作可查看作用域、差异、确认结果和撤销入口。
- 地图、时间轴和右栏选中状态同步；地图数据同时提供文本信息。
- 运行 `npm run type-check` 与 `npm run build` 通过，并在真实浏览器完成核心演示脚本。

后续页面若需要偏离本规范，应在对应设计说明中记录“偏离项、原因、影响和批准者”，不得静默引入第二套视觉语言。
